class Doctor
    Represents a doctor.

    def __init__(self, name, specialization)
        self.name = name
        self.specialization = specialization

    def __str__(self)
        return fDr. {self.name} ({self.specialization})
